server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yunming_tea?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ty302415.v
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.yunming.tea.entity

jwt:
  secret: yunming-tea-system-jwt-secret-key-2024-spring-boot-auth
  expiration: 86400000
